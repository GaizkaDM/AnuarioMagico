# CONFIGURACIÓN GENERAL DEL SISTEMA
# ------------------------------------------------------------------------------

# MODO DE BASE DE DATOS (OBLIGATORIO)
# Define qué tecnología usa el sistema.
# Opciones disponibles:
#   sqlite  -> Modo Local. Usa un archivo .db (Ideal para desarrollo o pruebas sin internet)
#   mysql   -> Modo Híbrido. Usa SQLite localmente pero permite sincronizar con una BD MySQL Remota.
DB_TYPE=sqlite

# ------------------------------------------------------------------------------
# CONFIGURACIÓN LOCAL (SOLO PARA DB_TYPE=sqlite O MODO HÍBRIDO)
# ------------------------------------------------------------------------------
# Ruta relativa al archivo de base de datos local
DB_SQLITE_FILE=data/anuario.db

# ------------------------------------------------------------------------------
# CONFIGURACIÓN REMOTA (SOLO PARA DB_TYPE=mysql)
# ------------------------------------------------------------------------------
# Rellena estos datos SOLAMENTE si vas a usar una base de datos MySQL para sincronizar.
# Si tu DB_TYPE es 'sqlite', estos valores serán ignorados.

# Dirección IP del servidor MySQL (ej: 127.0.0.1 para local, o IP remota)
DB_HOST=127.0.0.1

# Puerto de conexión (Por defecto MySQL usa 3306. Si usas Docker en clase, podría ser 3309)
DB_PORT=3306

# Nombre de la base de datos en el servidor
DB_NAME=hogwarts

# Usuario con permisos de lectura/escritura en la base de datos
DB_USER=root

# Contraseña del usuario
DB_PASSWORD=

# ------------------------------------------------------------------------------
# NOTA IMPORTANTE:
# Si cambias estos valores, DEBES reiniciar el servidor Backend para que surtan efecto.
# ------------------------------------------------------------------------------
